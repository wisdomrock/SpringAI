# Message Roles in LLMs: User,System, Assistant, Function
Spring AI's corresponding classes are: UserMessage, SystemMessage, AssistantMessage, ToolResponseMessage.

Spring AI Prompt Template, which can be stored in resources/promptTemplates/userTemplate.st (st stands for StringTemplate)



